Username: admin
Password: CYLnItFvs-OFz2l4
Use these for HTTP Basic Auth login to the dashboard.
To avoid random passwords on every start, set the env vars before launching:
  export DASHBOARD_USER=admin
  export DASHBOARD_PASS=your-chosen-password
